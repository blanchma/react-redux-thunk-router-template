import React, { useState } from "react";

const Home = () => {
  const [username, setUsername] = useState("blanchma");

  return (
    <div>
      <h1> Welcome to Repositories React App Template </h1>
    </div>
  );
};
export default Home;
